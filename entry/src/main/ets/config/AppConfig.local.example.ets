// Copy this file to AppConfig.local.ets and change the IP to your own computer's IPv4.
// AppConfig.local.ets is ignored by git, so everyone can keep their own backend address.
export const LOCAL_API_BASE_URL: string = 'http://127.0.0.1:8000/api/v1';
